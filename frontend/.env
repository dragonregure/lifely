# Docker Compose overrides these at container startup from root BACKEND_MODE.
# Set VITE_API_BASE_URL only when you need to target a fully custom API URL.
VITE_BACKEND_MODE=Nest
VITE_BACKEND_LARAVEL_PORT=8000
VITE_BACKEND_NESTJS_PORT=3000
# VITE_API_BASE_URL=http://localhost:8000/api/v1
